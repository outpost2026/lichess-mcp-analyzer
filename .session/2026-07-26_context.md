# Session Context — 2026-07-26

## System State

| Metrika | Hodnota |
|---------|---------|
| Branch | `main` (5ba1919) |
| Working tree | clean |
| Tests | 35/35 pass |
| Python | 3.12, uv |
| LLM cascade | NVIDIA (free) -> Cerebras (free) -> DeepSeek V4 Flash ($) |
| Stockfish | BMI2, Threads=6, Hash=512 |

## Git History (top of main)

```
5ba1919 Phase 1 fixes: P0 bugy, feat filter, docs, chore
e41ef52 chore: cleanup tracked artifacts, CI, build config
7f5e1ce docs: README, kontext, audit, planovaci dokumentace
04abb6f test: aktualizace na architekturu a featury
3bed835 feat: result filter a games index cache
c84f6d2 fix: P0 bugfixy — FEN+check detection, pattern J, engine, Lichess API auth
a916d56 main before merge (25.07.)
```

## P0 Completed (all verified in code)

| ID | Fix | File | Status |
|----|-----|------|--------|
| F-002 | fen v MoveAnalysis | `models/game.py:50` | ✅ `fen: str = ""` |
| F-003 | was_in_check pipeline | `models/game.py:51` + `game_analyzer.py` | ✅ `was_in_check: bool = False` |
| F-007 | pattern J semantika | `pattern_detector.py:191` | ✅ `m.was_in_check and "x" not in m.move_san` |
| N7 | sort before persist | `tools/match_patterns.py` | ✅ `result.sort()` before store |

## Next: P1 — DBCL Core Implementation

Priority: **HIGH** — builds on P0, unlocks structured fact extraction.

### P1.1: Inline context extraction v _run_analyze_pgn()

Misto samostatneho `context_extractor.py` (F-004), implementovat primo v `game_analyzer.py:_run_analyze_pgn()`:

1. Detekovat blunder window: delta > 300cp || classification in (blunder, mistake)
2. Extrahovat: FEN, is_check, legal_moves (captures/king_moves/blocks/checks)
3. Zavolat `engine_client.analyze_position(fen_before, depth=14, multipv=3)` — uz existuje
4. Zavolat per-blunder pattern matcher (B, J, S, R, C)
5. Sestavit BlunderFactSheet
6. Ulozit pod klic `"dbcl_fact_sheets"` v game_cache JSON

### P1.2: BlunderFactSheet schema

Navrh v `00_STRATEGIE/DBCL_cross_audit_artifact.md` §5.2:

```python
@dataclass
class BlunderFactSheet:
    fen_before: str
    fen_after: str
    was_in_check: bool
    legal_moves: LegalMovesSummary  # captures, king_moves, blocks, checks
    move_played: str                # SAN
    move_eval_before: float
    move_eval_after: float
    centipawn_loss: int
    classification: str
    engine_lines: list[EngineLine]  # multipv=3
    pattern_matches: list[str]      # pattern_ids triggered
    phase: str
    detector_version: str           # commit hash + timestamp
```

### P1.3: Prompt builder injection

- `llm_client.py:build_coaching_prompt()` — BlunderFactSheet[] misto agregovaneho blobu
- `game_llm_cache.py:_build_game_prompt()` — stejna zmena
- Guard clause template z DBCL §5.2.4 do system promptu

### P1.4: narrative_validator.py

5 kategorii claimu:
1. piece-on-square -> existence v fen_before
2. check -> rovnost proti was_in_check
3. capture -> existence v legal_moves.captures nebo engine_lines
4. eval-cislo -> tolerance +-20cp
5. legalita/negace -> negace proti legal_moves.*

Reject loop: if validator fail -> opakovat LLM call s guard clause.

### Key Files

| File | Role |
|------|------|
| `services/game_analyzer.py` | _run_analyze_pgn() — hlavni bod vkladani |
| `services/engine_client.py` | analyze_position(multipv=3) — existuje |
| `services/pattern_detector.py` | per-blunder pattern matcher call |
| `models/game.py` | MoveAnalysis uz ma fen + was_in_check |
| `models/analysis.py` | sem prida BlunderFactSheet |
| `services/llm_client.py` | build_coaching_prompt() — inject fact sheets |
| `services/game_llm_cache.py` | _build_game_prompt() — inject fact sheets |
| `services/narrative_validator.py` | NOVY: 5 kategorii claim operatoru |

### Testing

- Contract test: fact sheet schema + engine_lines mapping
- Pattern semantic contract: 1 pozitivni + 1 negativni pripad na detector per P2
- Halucinacni test: INC-A (kNAMNYUF) + INC-B (qmodxzNF) jako injection-ready data

### Reference Docs

| Doc | Ucel |
|-----|------|
| `docs/01_DBCL_unity_synthesis.md` | Synteza architektury + incident analysis |
| `docs/02_DBCL_meta_evaluation.md` | 3-chanel noise framework, epistemologicky ramec |
| `docs/PHASE2_BUILD_PLAN.md` | Build plan v2.0: P0-P3, blockers, sequence |
| `00_STRATEGIE/DBCL_cross_audit_artifact.md` | Zdrojova DBCL architektura §5.2, BlunderFactSheet v1.0 |
